# Target config — 256px, cosine LR with warmup, AMP. Aug pipeline added in Phase 4.
image_size: 256
batch_size: 16
epochs: 30
lr: 0.001
grad_clip: 1.0
lr_schedule: cosine
warmup_epochs: 3
use_amp: true
seed: 42
log_pred_every: 5
