# Baseline config — 128px, no augmentation, matches original reported results.
image_size: 128
batch_size: 16
epochs: 25
lr: 0.001
scheduler_step_size: 10
scheduler_gamma: 0.1
use_amp: false
seed: 42
